const livros = require('./listaLivros');
const menorValor = require('./menorValor');
const troca = require('./outraForma')
              // 1
for (let atual = 0; atual < livros.length - 1; atual++) {
  let menor = menorValor(livros, atual)

 troca(analise, lista)
}

console.log(livros)